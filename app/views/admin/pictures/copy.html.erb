<% @page_title = "Copy #{@picture.title} to another gallery" %>

<% tasks %> 

<% if @other_galleries.empty? %>
  <p>You have not another gallery where copy this picture</p>
  <p>You can <%= link_to 'create one', new_admin_gallery_url %></p>
<% else %>
  <h2>Copy picture to another gallery</h2>

  <%= render :partial => 'copy_form' %>
<% end %>
